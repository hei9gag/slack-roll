version: '3.1'
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
     - "5000:5000"
    volumes:
     - .:/app
     - /app/node_modules
    depends_on:
     - redis
  redis:
    image: redis:5.0.4-alpine